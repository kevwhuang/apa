---
import Layout from '@/Layout.astro';
import SettingsPanel from '@sections/settings/Panel.astro';
import { brandTitle } from '@lib/brand';
---

<Layout
    description={'Manage your Austin Producer Alliance profile \u2014 artist name, handle, bio, genres, city and state, links, main track, and password, plus your Prods balance.'}
    noindex
    title={brandTitle('Settings')}
>
    <SettingsPanel />
</Layout>
