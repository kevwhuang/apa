---
import ErrorServer from '@sections/errors/Server.astro';
import Layout from '@/Layout.astro';
import { brandTitle } from '@lib/brand';

export const prerender = false;
---

<Layout
    description="Something went wrong on our end. Try again in a moment, or head back to the Austin Producer Alliance home page."
    noindex
    title={brandTitle('Server Error')}
>
    <ErrorServer />
</Layout>
