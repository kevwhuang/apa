---
import ErrorServer from '@sections/errors/Server.astro';
import Layout from '@/Layout.astro';
import { brandTitle } from '@lib/shared/brand';

export const prerender = false;
---

<Layout
    description="Something failed on the Austin Producer Alliance server, not on your end. Reload in a moment, or use the links to home, events, the directory, or resources."
    noindex
    title={brandTitle('Server Error')}
>
    <ErrorServer />
</Layout>
