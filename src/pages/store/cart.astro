---
import CartView from '@sections/store/CartView.astro';
import Layout from '@/Layout.astro';
import { brandTitle } from '@lib/shared/brand';
---

<Layout
    description={'Review your Austin Producer Alliance cart \u2014 update quantities, remove items, and check the subtotal, estimated tax, and total before checkout.'}
    noindex
    title={brandTitle('Cart')}
>
    <CartView />
</Layout>
